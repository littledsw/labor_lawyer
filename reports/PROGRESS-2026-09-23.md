# 劳动仲裁律师助手 · 进度报告（2026-09-23 基线重算）

> 生成日期：2026-09-23 ｜ 对应提交：`labor_lawyer` 本轮「案例语料结构化」提交（父提交 `a4ece23`；
> 定位方式 `git log -- reports/PROGRESS-2026-09-23.md`）· `legal-assistant` `3559830`
> 按 `reports/README.md` 约定「只增不改」：**不覆盖** `PROGRESS-2026-09-21.md`，本报告只做基线重算、差异说明与本轮新增。
> 阅读顺序建议：先读 [PROGRESS-2026-09-21.md](./PROGRESS-2026-09-21.md) 了解架构与口径决策（§一—§六、§十 仍然有效），
> 再看本报告的差异与新进展；缺口清单以本报告 §四 为准。

---

## 一、与 09-21 报告的差异（数字修正）

09-21 报告的基线数字是「北京 + 国家层面首轮归档」时的快照，其后 `stage18/19/20` 上线与省市批量归档都没有回写报告。
本次按仓库实际内容重算：

| 项 | 09-21 报告 | 本次重算 | 差异原因 |
| --- | --- | --- | --- |
| `indexes/documents.*` 记录数 | 275 | **882** | `stage20_flk_npc.py` 全量接入国家法律法规数据库（614 件）+ 各省市地方法规批量归档 |
| 磁盘受控文件数（`verify.py` 口径） | 291 | **964** | 同上；含各附件 `.meta.yaml` 与官方原件 |
| git 跟踪文件数 | 446 | **1448** | 同上 + 派生数据与工具链 |
| 覆盖地区数 | 2（北京、国家） | **67** | 国家 1 + `municipalities/` 5（北京、天津、上海、重庆、**山西**——按既有归档口径归在此目录）+ 省 25 + 地级市 32 + 自治区/自治州 4 |
| `topic=regulations` | 6（北京）+ 国家 80 | **689** | flk 全量（法律 / 行政法规 / 司法解释 / 地方性法规 / 地方政府规章） |
| 案例切片（`cases-chunks.jsonl`） | 80 | **168** | 切片口径修正 + 单篇案例 + 2017 年度合集（见 §二） |
| 结构化案例（`cases-structured.json`） | 80 | **168** | 同上 |
| 法条引用（结构化记录内） | — | **315 条 / 173 部法条与文件** | 新增维度 |
| 状态分布 | `active` 274 · `superseded` 1 | `active` **879** · `not-yet-effective` **2** · `superseded` **1** | flk 只收「有效 / 尚未生效」 |
| `legal-assistant` 测试 | 75 项 | **102 项**（全部通过） | 新增社保损失赔偿模块（`social-insurance-loss.yaml` + 6 项守卫测试等） |
| `legal-assistant` 规则 / 代码 | 6 条规则 | **7 条规则 / `src` 1328 行** | 新增社保损失；口径文档 3 篇（`knowledge-base/laws/`） |

> 09-21 报告 §三（参数层）、§五（数据质量纪律）、§六（工具链）、§十（复现命令）内容**仍然有效**，
> 只需把其中的规模数字按上表替换；§七 缺口清单见本报告 §四。

---

## 二、本轮新增：案例语料结构化补齐

此前只有「年度十大案例合集」被结构化（80 条切片），**78 篇单篇案例完全没有切片**，检索时命中不到。
本轮把切片口径整体修正并跑完抽取：

### 2.1 切片口径修正（`tools/crawl/extract_cases.py`）

| 问题 | 修正后 |
| --- | --- |
| 主题目录 `cases/README.md` 被当成案例，污染法条引用与主题索引 | 不进入派生数据 |
| 年度案例的 2 个「官方发布动态」页（首都之窗转发页）被当成案例 | 标 `kind: 发布动态`，不计入引用 / 主题索引 |
| 4 个专题页正文采集失败、只剩站内搜索控件文案，被当成案例 | 新增识别 → `kind: 空壳页`，不产切片，并由 `stage21` 在正文顶部标注 |
| 单篇案例无切片（78 篇） | 单篇案例本身即一个语义单元 → 每篇切 1 片 |
| 2017 年度合集（「典型案例之一/之二……」+ 加粗标题）切不出片（0 片） | 新增顺序编号切分逻辑 → 10 片 |

切片结果：**168 条**（年度合集 9 个 × 10 片 = 90，单篇案例 78 篇 × 1 片 = 78），逐条保留
`parent` / `source_url` / `local_path`，命中后可回溯官方原文。

### 2.2 模型抽取与校验

- 抽取批次 9 个（`indexes/derived/llm-extract/2017-07-19.json` + `singles-01..08.json`），
  由 9 个子代理并行完成，输入 92 条切片（含 4 条空壳页，入库前按新口径剔除）→ 实际入库 **88 条**。
- `merge_llm_extract.py` 校验：**切片覆盖 168/168**、11 个字段齐备、`keywords ≤ 5`；
  **315 条法条引用全部通过原文字面回文核验**（`exact` / `short-form` / `anaphoric` 三档，0 条 `unverified`）。
- 字段空值分布：`legal_basis` 空 16 条（原文确实无法条引用）、`amounts` 空 51 条（原文无金额或工资口径），
  其余字段（争议焦点 / 请求 / 结果 / 要旨 / 关键词 / 主体类型）168 条齐全。

### 2.3 新增可复现脚本

`tools/crawl/stage21_flag_empty_cases.py` —— 为正文缺失页加缺失提示并重写 `notes`（幂等，`--render-check`
用 headless Chrome 复核），已接入 `run_all.sh`。

---

## 三、当前知识库索引现状（回答「有没有建索引」）

三层结构，**前两层已完整、第三层刚补齐，但都还没有检索入口**：

| 层 | 产物 | 现状 |
| --- | --- | --- |
| 元数据层（权威、Agent 检索默认入口） | `indexes/documents.json` / `documents.csv`（882 条 × 14 字段）、`originals-sha256.json`（原件指纹） | ✅ 由 `stage6_indexes.py` 生成，`verify.py` 校验「索引行数 = manifest 记录数 = 磁盘文件数」 |
| 主题视图（人读） | `indexes/by-topic/` 8 个 md 清单 | ✅ 脚本生成，勿手改 |
| 派生块（喂模型） | `cases-chunks.jsonl`（168 片）、`cases-structured.json` / `.md`（168 条）、`cases-citations.md`（173 部）、`cases-topics.md`（20 类）、`cases-meta.json`、`llm-extract/` 17 个批次文件 | ✅ 本轮补齐 |
| **全文索引 / 向量索引** | — | ❌ **两者都没有**（全仓库无 faiss / chromadb / sqlite / bm25 等依赖与产物） |
| **检索入口（读派生数据的代码）** | — | ❌ `legal-assistant` 目前只读 `regions/*/statistics/parameters.yaml`（`calculations/src/params.py` 经 `LABOR_LAWYER_REPO`），**从不读取 `indexes/`** |

判断：当前规模（882 条记录、`regions/` 下 574 篇 md、最大单文件 276 KB）下，**元数据过滤 + 命中文件直读**优于向量 RAG；
精确标识符（文号、条号）本来就不该交给向量。向量检索只在「按事实模式找相似案例」成为真实需求时再上，
且应采用「元数据预过滤 → 关键词/BM25 → 向量重排」的混合方案、embedding 本地跑。

---

## 四、缺口清单（以本清单为准，替换 09-21 报告 §七）

| # | 缺口 | 状态 / 现状 |
| --- | --- | --- |
| 1 | 2025 年度法人单位从业人员平均工资（三倍封顶基数） | 未发布；估算路径就绪（`stage16`） |
| 2 | 工伤待遇社平口径 | `pending-official-confirmation`（需向社保经办 / 12333 核实） |
| 3 | 调休上班日（补班日）加班费定性 | `pending-official-confirmation`（94 篇案例 + 168 条切片中无一篇涉及，负面证据已穷尽） |
| 4 | 京政发〔1995〕14号、京劳社资发〔2003〕157号 原文 | 公开渠道未定位到官方页 |
| 5 | 2016—2021 最低工资调整通知原件 | 需逐年检索人社局政策文件栏目 |
| 6 | 单篇案例结构化 | ✅ **已闭合**（原缺口 #7；实际为 78 篇单篇 + 2017 合集 10 条） |
| 7 | **4 个正文缺失页**（2024 年度案例专题页 `t20241231_3977502/3977632/3977640/3977650`） | 站点 JS 加载，静态与渲染抓取均只得到 228 字符站内搜索控件；**同案正文已在 2024 年度十大案例合集归档并完成抽取**，故检索不受影响；文件本身不可引用，已由 `stage21` 标注 |
| 8 | **两处原文本身残缺（未修改原文）** | ① `2016-06-28-合同已签…`：案情写「2016年4月10日签订」、评析写「2013年4月10日签订」，年份自相矛盾；② `2022-11-17-用人单位不得以任何理由收取劳动者财物`：案情「要求全额返还项目风险抵押金.照项目进度及实际产值按比例扣除…」处明显脱字。均属官方页面原文问题，只在报告登记 |
| 9 | **派生数据没有检索入口** | `documents.json` 的元数据过滤（region/topic/status/published_at）与 `cases-structured.json` 的读取尚无代码；接入后 Agent 才真正用得上本轮语料 |
| 10 | 其他省市计算参数表 | 仅北京、山西、国家层面三张 `parameters.yaml`；其余 64 个地区只有法规原文，无参数 |
| 11 | 文书生成 | 模板 → 规则 → 金额 → 依据的打通仍未做 |

---

## 五、复现与验证（本报告数字的来源命令）

```bash
export LABOR_LAWYER_REPO=~/workspace/projects/labor_lawyer
export LABOR_LAWYER_CACHE=~/workspace/_agents/hermes/labor_lawyer_crawl
V=$LABOR_LAWYER_CACHE/.venv/bin/python

# ① 归档与索引一致性（基线数字）
cd "$LABOR_LAWYER_REPO/tools/crawl" && $V verify.py
#   → records=882 csv=882 json=882 disk_files=964；校验通过：无异常

# ② 案例派生与结构化
$V extract_cases.py         # → 文件 93（单篇 78/年度合集 9/发布动态 2/空壳页 4）；切片 168
$V merge_llm_extract.py     # → 切片 168 / 抽取 168 / 覆盖 168；法条 315 条全部回文核验通过
#   （抽取批次输入与规范：~/workspace/_agents/hermes/labor_lawyer_crawl/）

# ③ 正文缺失页复核
$V stage21_flag_empty_cases.py --render-check

# ④ 计算模块
cd ~/workspace/projects/legal-assistant
LABOR_LAWYER_REPO=~/workspace/projects/labor_lawyer .venv/bin/python -m pytest calculations -q
#   → 102 passed
```

**回归基线（供下次比对）**：labor_lawyer 记录 **882**（= csv = json = manifest）· 磁盘受控文件 **964** ·
git 跟踪 **1448** 文件 · 案例切片 **168** · 结构化案例 **168** · 法条引用 **315** · legal-assistant 测试 **102**。

---

**免责声明**：本仓库仅归档公开资料并提供计算工具，不构成法律意见。模型抽取的争议焦点 / 要旨属归纳性内容，
法条依据保留原文写法并经回文核验，但正式用于出具意见或文书前仍需对照 `local_path` / `source_url` 的官方原文复核。
